<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>
    <!-- Add Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Add your custom CSS -->
    <link href="styles.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">Weather App</h1>
        <form action="/" method="get" class="mb-4">
            <div class="input-group">
                <input type="text" class="form-control" id="city" name="city" placeholder="Enter City Name" aria-label="Enter City Name" aria-describedby="button-addon2">
                <button class="btn btn-primary" type="submit" id="button-addon2">Get Weather</button>
            </div>
        </form>
        <div>
            <% if (locals.weatherData) { %>
                <h2 style="color: red;">Weather in <%= city %> :</h2>
                <p>Temperature: <%= weatherData.main.temp %>°C</p>
                <p>Weather: <%= weatherData.weather[0].description %></p>
                <p>Humidity: <%= weatherData.main.humidity %>%</p>
                

            <% } else { %>
                <p>No weather data available</p>
            <% } %>
        </div>
    </div>

    <!-- Optional: Add Bootstrap JS if you need it -->
    <!-- <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script> -->
</body>
</html>
